# see each package/package.json.config for the default env of each microservice
# create a pkg/.env to override the default env values
# each pkg.json.config values are injected into the .env at build/run time
